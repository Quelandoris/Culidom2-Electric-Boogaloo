using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class BiddingButtonToggle : MonoBehaviour {

    /*string[] gameObjectName;
    int playerNum;
    Image playerButton;
    Vector4 toggled = new Vector4(1f, 1f, 1f, 0.85f);
    Vector4 unToggled = new Vector4(1f, 1f, 1f, 1f);

    // Use this for initialization
    void Start () {
        playerButton = this.gameObject.GetComponent<Image>();
        gameObjectName = this.gameObject.name.Split(' ');
        playerNum = int.Parse(gameObjectName[1]);
	}
	
	// Update is called once per frame
	void Update () {
        switch (playerNum) {
            case 1:
                if (Input.GetKeyDown(KeyCode.Q))
                {
                    playerButton.color = Color.red;
                }
                break;
            case 2:
                if (Input.GetKeyDown(KeyCode.X))
                {
                    playerButton.color = Color.red;
                }
                break;
            case 3:
                if (Input.GetKeyDown(KeyCode.N))
                {
                    playerButton.color = Color.red;
                }
                break;
            case 4:
                if (Input.GetKeyDown(KeyCode.P))
                {
                    playerButton.color = Color.red;
                }
                break;
        }

        switch (playerNum)
        {
            case 1:
                if (Input.GetKeyUp(KeyCode.Q))
                {
                    playerButton.color = Color.white;
                }
                break;
            case 2:
                if (Input.GetKeyUp(KeyCode.X))
                {
                    playerButton.color = Color.white;
                }
                break;
            case 3:
                if (Input.GetKeyUp(KeyCode.N))
                {
                    playerButton.color = Color.white;
                }
                break;
            case 4:
                if (Input.GetKeyUp(KeyCode.P))
                {
                    playerButton.color = Color.white;
                }
                break;
        }
    }*/
}
