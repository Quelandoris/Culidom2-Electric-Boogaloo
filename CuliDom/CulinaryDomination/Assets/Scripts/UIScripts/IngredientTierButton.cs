using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class IngredientTierButton : MonoBehaviour {
    public RecipeIngredient ingredient;
    public int tier;


    /*public void Update()
    {
        if (tier != tier) // the second tier would be overall tier
        {
            gameObject.GetComponent<Image>().color = new Color(1f, 1f, 1f, 0.2f);
        }
    }*/
}
