using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class UpdateRestaurantName : MonoBehaviour
{

    /*public InputField nameText;
    public bool isUpdating = false;

    public void Update()
    {
        if (!isUpdating)
        {
            nameText.text = GameController.Instance().activePlayer.restaurantClicked.name;
        }
    }

    public void BeginNameUpdate()
    {
        isUpdating = nameText.isFocused;
    }

    public void UpdateName()
    {
        GameController.Instance().activePlayer.restaurantClicked.name = nameText.text;
        isUpdating = false;
    }*/

}
